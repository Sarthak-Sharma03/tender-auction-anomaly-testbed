# Convenience requirements for running the core pipeline without an editable install.
#
# Recommended install:
#   python -m pip install -e ".[dev]"
#
# Core runtime dependencies:
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3
openpyxl>=3.1
matplotlib>=3.8
pyyaml>=6.0

# Optional (serving the reference API):
# fastapi>=0.110
# uvicorn>=0.23
